<core:FragmentDefinition
	xmlns="sap.m"
    xmlns:f="sap.ui.layout.form"
    xmlns:l="sap.ui.layout"
	xmlns:core="sap.ui.core">
        <Panel>
            <Text text="Username" />
            <Input value="{$this>username}" valueLiveUpdate="true" change="onChange" />
            <Text text="Password" type="password" />
            <Input value="{$this>password}" valueLiveUpdate="true" />
        </Panel>
</core:FragmentDefinition>